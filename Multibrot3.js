function setup(){
    createCanvas(720,720);
    pixelDensity(1)
}

function draw(){
    //todo create a 3rd degree mandelbrot that appears on button press. potentially general formula but we will see
}